# 2026-06-11

## C2 SMART - controleur de scenes unifie

- Cree `apps/web/src/components/smart/SceneController.tsx`.
- Branche la sidebar SMART sur une grille de scenes 4 pages x 16 slots, avec vue Visuel et vue MIDI 8x8.
- Ajoute `page` / `slot`, `activePadPage`, `smartPadViewMode`, `movePad`, `applyPadTemplate` dans `smartModeSlice`.
- Migre les anciens mappings MIDI `pad_<id>` vers `pad_<page>_<slot>` via hydratation Zustand et `projectMigration`.
- Retire les widgets centraux legacy `pads` et `apcVirtual` du dashboard SMART.
- Adapte `MidiListener` pour le learn, le feedback LED et le declenchement des pads page/slot.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C3 SMART - inspecteur contextuel

- Cree `apps/web/src/components/smart/FixtureInspectorPanel.tsx` pour partager les controles projecteur entre Creator et sidebar SMART.
- Remplace `FixtureController.tsx` par un wrapper de page qui conserve la selection projecteur et rend `FixtureInspectorPanel layout="page"`.
- Cree `apps/web/src/components/smart/GroupInspectorPanel.tsx` avec intensite, couleur, mute et liste des projecteurs assignes.
- Branche `SmartSidebar.tsx` sur le mode inspector fixture/groupe avec retour Scenes, fermeture et Escape.
- Modifie `SmartDashboard.tsx` : le Plan de Scene ouvre la sidebar inspector, le panneau groupe local est supprime.
- Adapte `VisualizerView.tsx` pour ouvrir le meme inspecteur sidebar en mode SMART.
- Refactorise `MidiListener.tsx` en table de routes et ajoute les controles `fixture_<nodeId>_pan`, `_tilt`, `_dimmer`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C4 SMART - REC automation Pan/Tilt

- Ajoute `automationRecArmed`, `recTargetFixtureId`, `timelinePlaying`, `playheadMs` et les actions `getOrCreateAutomationTrack` / `recordKeyframeBatch` dans `timelineSlice`.
- Ajoute `AutomationEasing` sur les keyframes et l'interpolation `linear`, `easeIn`, `easeOut`, `easeInOut`, `hold` dans `MacroTimeline`.
- Cree `apps/web/src/lib/simplifyKeyframes.ts` pour simplifier les courbes REC avant insertion.
- Ajoute le bouton REC dans `FixtureInspectorPanel` sidebar: punch-in sur drag XY, capture 50 ms, creation/overwrite des pistes Pan/Tilt, release des locks manuels, toast de confirmation.
- Ajoute badge `REC AUTO`, highlight rouge des pistes cibles, menu clic droit keyframe avec easing + suppression, et select easing dans l'inspecteur automation.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C9 SMART - fiabilite sorties DMX

- Cree `apps/server/services/outputHealth.ts` pour suivre `python`, `qlcOsc`, `qlcWs`, `artNet`, `usbDmx` avec etat `ok/degraded/error/off`, timestamps, compteur d'erreurs et logs support throttles.
- Refactorise `apps/server/services/dmxRouter.ts` pour reporter `reportOk/reportError` par sortie au lieu d'avaler les exceptions silencieusement, sans emission socket par canal.
- Ajoute `GET /api/dmx/output-status`, emission socket `dmx_output_status` a la connexion, au changement et heartbeat 5 s dans `apps/server/index.ts`.
- Ajoute les types `DmxOutputHealthSnapshot` cote web et le champ non persiste `dmxOutputHealth` dans `smartModeSlice`.
- Cree `apps/web/src/components/smart/DmxStatusBadge.tsx` avec badge TopBar, popover detail par sortie, mention UDP best-effort et toasts anti-spam sur transition erreur/retablissement.
- Remplace l'ancien badge DMX statique de `TopBar.tsx` par `DmxStatusBadge`.
- Etend `apps/server/tests/api-smoke.ts` pour couvrir le nouveau snapshot `/api/dmx/output-status`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C5 SMART - groupes DMX dynamiques

- Refactorise `showPlayerSlice.ts` avec `DmxGroup`, `GroupPreset`, migration des anciens groupes fixes vers des ids (`face`, `douche-1`, etc.) et niveaux/mutes/couleurs re-cles par id.
- Ajoute sync optimiste avec `/api/fixture-groups` pour chargement, creation, edition couleur/nom/fixtures et suppression.
- Cree `apps/web/src/components/smart/GroupStrips.tsx` : faders dynamiques, Master Dimmer, VU-metres 10 Hz, MIDI learn `group_<id>_level` / `_mute`, edition, suppression, reorder et presets.
- Remplace le rendu groupes de `SmartDashboard` par `GroupStrips` et emet les intensites uniquement pour les groupes avec `backendZone`.
- Met a jour `GroupInspectorPanel` pour resoudre les groupes dynamiques et assigner les fixtures via checkboxes.
- Met a jour `MidiListener` : routes `group_`, auto-map APC par ordre de `dmxGroups`, feedback LED des mutes dynamiques.
- Ajoute `dmxGroups` / `groupPresets` a la persistance Zustand et a la sauvegarde/projet via `projectSlice` et `projectMigration`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK, 0 warning.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C6 SMART - plan de scene editable + preview

- Cree `apps/web/src/components/smart/StagePlan.tsx` : plan normalise 0-100, drag en mode Edit avec snap 2%, retrait clic droit, ajout de fixtures hors plan, selection fixture/groupe vers l'inspecteur.
- Ajoute `apps/web/src/lib/stagePlanMapping.ts` et branche `FixtureRenderer.tsx` sur `fixtures[].gridPosition` via `stagePlanToWorld`.
- Ajoute dans `smartModeSlice` : `stagePlanHidden`, `stagePlanEditMode`, `previewMode`, `setFixtureGridPosition`, `addFixtureToPlan`, `removeFixtureFromPlan`, `setPreviewMode`.
- Le mode Preview sauvegarde/restaure `dmxEngine.getOutputGate()` et coupe les sorties via `setOutputGate({ all false })`, sans toucher au moteur 44 Hz.
- Persiste `stagePlanHidden` dans Zustand et les projets, schema projet passe en v5.
- Ajoute persistance serveur de `gridPosition` sur les fixtures (`grid_position` SQLite, `/api/fixtures`).
- Remplace l'ancien plan hardcode du dashboard par `StagePlan`, ajoute le highlight groupe dans `GroupStrips`, et affiche Preview dans `VisualizerView` + `DmxStatusBadge`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Verification navigateur: app ouverte sur `http://localhost:3000`, `Plan de scene` et controles Preview visibles. Backend non lance donc logs attendus `Failed to fetch fixtures`.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C8a SMART/CREATOR - AppShell commun

- Cree `apps/web/src/components/AppShell.tsx` a partir de la coque principale existante: TopBar, MidiListener, Toasts, GuidedTour, zone centrale Smart/Creator et MacroTimeline vivent maintenant dans un composant partage.
- Simplifie `apps/web/src/app/page.tsx` pour rendre `<AppShell />`.
- Simplifie `apps/web/src/app/smart/page.tsx` pour rendre `<AppShell routeMode="smart" />`; la route `/smart` conserve donc le raccourci PWA sans dupliquer la coque.
- Preserve le support `?mode=smart|creator|live` sur `/` dans AppShell.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Verification navigateur: `/` et `/smart` affichent `GLOW`, `SMART`, `Timeline` et une zone dashboard/canvas. Les seules erreurs console relevees sont les fetch fixtures attendus quand le backend local n'est pas lance.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C8b SMART/CREATOR - sidebar 3 roles + bibliotheque

- Etend `smartSidebarPanel` a `scenes | inspector | library` dans `uiSlice` et la migration Zustand.
- Refactorise `SmartSidebar.tsx` avec un rail vertical 3 roles: Scenes, Inspecteur, Bibliotheque.
- Ajoute le role Bibliotheque integre: fixtures patchees drag vers canvas, palette de noeuds en mode Creator/Build, items `/api/library` (fixtures, looks, shows, venues), import JSON, creation de look utilisateur, medias playlist.
- Remplace la sidebar Creator dans `AppShell` par `SmartSidebar`; l'ancienne `components/Sidebar.tsx` est supprimee.
- Transforme `LibraryModal` en raccourci compatible qui ouvre la sidebar Bibliotheque, et la TopBar ouvre directement ce role.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK, 0 warning.
- Verification navigateur: en mode Creator, clic rail `Bibliotheque` affiche `Fixtures patchees`, `Noeuds`, `Looks & presets`, `Medias`.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C8c SMART/CREATOR - fusion plans de feu

- Ajoute `capabilities?: "perform" | "build"` a `StagePlan`.
- Remplace l'onglet Creator `FIXTURES` dans `AppShell` par `<StagePlan capabilities="build" />`.
- Rend `SmartSidebar` visible en Creator pour les vues canvas, patch/build et 3D.
- Ajoute un panneau lateral Build dans `StagePlan`: metriques fixtures/visibles/hors plan/groupes, ouverture Bibliotheque, activation edition, rappel de migration `gridPosition`.
- Supprime les champs legacy `x3d/y3d/z3d` du type `NodeData`, de `reactFlowSlice.updateFixturePosition` et de la normalisation fixtures SMART.
- Verification `rg "x3d|y3d|z3d" apps/web/src apps/server` vide.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Verification navigateur: en Creator, bouton `FIXTURES` affiche `Plan de feu Build`, `Patch Build` et le rappel `gridPosition`; les signaux de l'ancien PatchPanel ne sont plus rendus dans cette vue.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C8d SMART/CREATOR - selection unique

- Ajoute l'action centrale `selectFixture(id | null)` dans `uiSlice`: met a jour `selectedFixtureId`, `selectedFixtureIds`, nettoie le groupe selectionne et ouvre la sidebar Inspecteur.
- Branche `selectFixture` sur le canvas ReactFlow (`AppShell`), le visualiseur 3D (`VisualizerView` / `FixtureRenderer`), le plan de scene (`StagePlan`), le controleur fixture, l'inspecteur groupe et la bibliotheque sidebar.
- Le `selectedNode` ReactFlow reste mis a jour pour les noeuds canvas, mais les noeuds fixture synchronisent aussi la selection fixture canonique.
- Le ring 3D lit maintenant `selectedFixtureId` en plus de `selectedNode`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api` OK.
- Verification navigateur: en Creator/Canvas, clic sur le noeud `PAR LED 1` ouvre l'Inspecteur dans la sidebar commune; le rail Scenes/Inspecteur/Bibliotheque reste visible.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C8e SMART/CREATOR - undo global

- Cree `apps/web/src/store/history.ts`: historique non persiste de commandes undo/redo, limite 100 entrees, coalescing court pour les drags, replay guard et profondeur verrouillee par `showLock`.
- Branche `Ctrl+Z`, `Ctrl+Y` et `Ctrl+Shift+Z` dans `AppShell` avec toasts; les champs input/textarea/select/contenteditable gardent leur comportement natif.
- Nettoie l'historique au chargement projet et a la restauration snapshot (`projectSlice`), et synchronise `showLock` avec `setHistoryLock`.
- Ajoute les entrees d'historique pour pads SMART (edition, suppression, deplacement), plan de scene (deplacement fixture, ajout/retrait plan), timeline (ajout/suppression/deplacement/redimension clip), automations (tracks et keyframes), groupes DMX dynamiques (creation, renommage/modif, suppression, reorder) et ReactFlow (ajout/suppression noeud/lien, connect).
- Conserve les faders/DMX temps reel hors historique; les mouvements timeline/automation/plan sont coalesces pour eviter des dizaines d'undos pendant un drag.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api`, `git diff --check` OK.
- Lint workspace global: echec attendu car `server@1.0.0` n'a pas de script `lint`; lint web cible OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.
- Verification navigateur non relancee: lancement du dev server web bloque par le systeme d'approbation dans cette session.

## C10 SMART - securite API locale + safety gate bloquant

- Cree `apps/server/middleware/auth.ts`: generation token 32 octets hex, stockage `api_token`, `allowed_origins`, verification Bearer avec `timingSafeEqual`, bootstrap loopback et auth Socket.IO.
- Remplace CORS `*` par les origines autorisees, ajoute `GET /api/health`, `POST /api/auth/bootstrap`, `GET /api/auth/token`, `POST /api/auth/regenerate`; regeneration deconnecte les sockets.
- Active `requireAuth` sur `/api/*` sauf health/bootstrap; serveur ecoute explicitement sur `0.0.0.0`.
- Frontend: `lib/config.ts` resout le token depuis `localStorage` puis bootstrap local, patch `window.fetch` pour ajouter `Authorization`, expose `apiFetch`, gere tablette via `http://<host>:3005`; `socket.ts` envoie le token au handshake.
- Ajoute un ecran d'appairage minimal dans `AppShell` si le bootstrap local echoue, et une section `Appairage API locale` dans Reglages > Safety Gate avec affichage/masquage et regeneration token.
- Safety: armement laser/pyro/drone/external_api expire apres 30 minutes, `show-actions` renvoie 403 et stoppe sur action dangereuse refusee, emission `safety_status`; TopBar synchronise et notifie les desarmements.
- DMX: clamp centralise dans `dmxRouter` des canaux appartenant aux fixtures detectees laser/pyro/flame/firework tant que les sorties physiques dangereuses restent desactivees.
- Tests API mis a jour: bootstrap token, 401 sans token sur route protegee, routes smoke avec Bearer, show-action dangereuse attendue en 403.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api`, `git diff --check` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.

## C7 SMART - trajectoires dessinees vers keyframes pan/tilt

- Cree `apps/web/src/lib/trajectoryGenerator.ts`: generateur pur pour `circle`, `eight`, `sweep`, `free`, valeurs DMX clampees, repetitions 1-64, sweep avec easing et tracé libre simplifie.
- Cree `apps/web/src/components/smart/TrajectoryEditor.tsx`: modal 640px max avec canvas Pan/Tilt, formes Cercle/Huit/Sweep/Libre, preview anime, Sync BPM local, duree secondes, repetitions, bouton Tester et insertion timeline.
- Branche `TrajectoryEditor` dans `FixtureInspectorPanel` via un bouton `Trajectoire` visible quand le projecteur a pan/tilt.
- L'insertion utilise `getOrCreateAutomationTrack` + `recordKeyframeBatch` a partir du `playheadMs`; la fenetre overwrite vaut `cycleDurationMs * repetitions`.
- Le bouton Tester joue une fois en live avec `dmxEngine.setChannel(..., { source: "manual", lockMs: 0 })` sans ecrire dans la timeline.
- Ajoute `apps/web/tests/trajectoryGenerator.test.ts` et le script `npm run test:unit --workspace web`; test couvre le cercle 16*kf/cycle +1, clamp, premier point, et sweep round-trip avec easing.
- Validation: `npm run test:unit --workspace web`, `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:api`, `git diff --check` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM`; execution hors sandbox demandee mais refusee par l'infra d'approbation.
- Verification navigateur non relancee dans cette session.

## C11 qualite - scenario Playwright show de zero

- Cree `apps/web/tests/helpers/stubBackend.ts`: mock backend Playwright partage, avec bootstrap auth local, routes safety/sync/DMX, fixtures, groupes, bibliotheque, scenes et projets persistants en memoire.
- Refactorise `apps/web/tests/visual.spec.ts` pour utiliser ce helper au lieu de dupliquer les routes API.
- Ajoute `apps/web/tests/show-from-scratch.spec.ts`: scenario cible "Show de zero" automatise en premiere tranche: generation des pads mariage, conversion `Pads -> Arrangement`, sauvegarde projet via modal, generation d'un autre template, rechargement du projet sauvegarde et verification des pads/timeline restaures.
- Ajoute les selecteurs stables/accessibles minimum: `aria-label` et `data-testid` sur les pads scene, `aria-label` sur les controles projet, `data-testid` sur les lignes projet, classe `.timeline-clip` sur les clips.
- Ajoute scripts web `test:e2e`, `test:visual`, `test:show`; declare `@playwright/test` dans `apps/web/package.json`.
- Validation OK: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit --workspace web`, `npm run test:api`, `git diff --check`.
- Playwright non executable localement tant que `@playwright/test` n'est pas installe dans `node_modules`; tentative `npm install -D @playwright/test@1.60.0 --workspace web` refusee par l'infra d'approbation.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM` identique aux chantiers precedents.

## C11 qualite - consolidation blackout

- Unifie la resolution `blackout` / `smartBlackout` comme un alias de compatibilite: si l'un des deux est actif, les deux sont restaures actifs.
- Corrige `projectMigration.ts` pour ne plus pouvoir recharger un projet avec `blackout=false` et `smartBlackout=true` (ou inversement).
- Corrige la rehydratation Zustand pour utiliser la meme regle OR et resynchroniser les deux champs.
- Corrige les snapshots projet pour inclure les champs ajoutes recemment: `dmxOutputs`, `networkState`, `masterDimmer`, `blackout`, `smartBlackout`, `laserArmed`, `pyroArmed`.
- Corrige sauvegarde projet et export/import `.glowproject` pour ecrire un etat blackout canonique.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit --workspace web`, `npm run test:api`, `git diff --check` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM` identique aux chantiers precedents.

## C11 qualite - CI Playwright

- Ajoute les scripts racine `test:unit`, `test:visual`, `test:show`, `verify:ui`, `verify:all`.
- Ajoute `.github/workflows/ci.yml`: checkout, Node 24, `npm install`, installation Chromium Playwright, `npm run verify`, unit, API smoke, build, visual smoke, show-from-scratch, upload du rapport Playwright.
- Durcit `apps/web/playwright.config.ts` pour CI: `forbidOnly`, retries, worker unique, reporter HTML/GitHub.
- Validation locale OK: `npm run verify`, `npm run test:unit`, `npm run test:api`, `git diff --check`.
- `npm run test:show` trouve maintenant Playwright et demarre, mais echoue sur `spawn EPERM` au lancement du worker Playwright; execution hors sandbox demandee (`npm run test:show`) et refusee par l'infra d'approbation.

## C12 offline - queue DMX + snapshot projet local

- Ajoute une queue DMX offline dans `apps/web/src/lib/dmxEngine.ts`: quand Socket.IO est deconnecte, le moteur conserve le dernier ordre par `universe:channel` dans `localStorage` et le rejoue a la reconnexion avant le resync complet.
- Expose `getOfflineQueueStatus()` et l'evenement `glowlogic:dmx-queue` pour afficher le nombre d'ordres en attente et le dernier flush.
- Ajoute `apps/web/src/lib/offlineProjectBackup.ts` et `apps/web/src/hooks/useOfflineProjectBackup.ts`: snapshot projet local toutes les 15 s + `visibilitychange/pagehide/beforeunload`, independant du backend.
- Branche `useOfflineProjectBackup()` dans `AppShell`.
- Etend `useOfflineReadiness()` avec `offlineProjectBackup` et `dmxQueue`; `ready` exige maintenant un snapshot local de show en plus de PWA/projet/licence/bibliotheque.
- Etend le badge Offline de `TopBar`: affiche snapshot local (age + pads) et queue DMX (pending/dernier sync).
- Met a jour les checks `SmartDashboard` Offline/Recovery avec snapshot local et queue DMX.
- Ajoute `apps/web/tests/offlineProjectBackup.test.ts` et l'integre a `npm run test:unit`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit`, `npm run test:api`, `git diff --check` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM` identique aux chantiers precedents.
