## C12b offline - restauration du snapshot local

- Ajoute la restauration du backup `glow-logic-offline-project-backup` depuis l'outil `Recuperation de show`.
- `SmartDashboard` lit le backup offline, demande confirmation, migre l'etat via `normalizeProjectState`, nettoie l'historique global avec `clearHistory`, puis applique l'etat restaure dans Zustand.
- `SmartToolsModals` affiche maintenant le snapshot offline comme metrique de recovery et expose le bouton `Restaurer offline`.
- Ajoute un check `Snapshot offline` dans la checklist recovery avec age, nom de projet et nombre de pads.
- Durcit `projectMigration.ts`: `dmxOutputs` est normalise avec les 4 champs stricts (`qlcOsc`, `qlcWs`, `artNet`, `usbDmx`) au lieu d'un record trop large.
- Etend `offlineProjectBackup.test.ts` pour tester la restauration/migration du backup local: nom projet, pads, clips, masterDimmer et synchro blackout/smartBlackout.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit`, `npm run test:api`, `git diff --check` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM` identique aux chantiers precedents.

## C12c launcher/status terrain

- Ameliore `scripts/Launch-GlowLogic.ps1`: ajoute `API_HEALTH_URL`, `Test-HttpEndpoint`, `Wait-Http` et `Get-LanIpv4`.
- Le launcher attend maintenant `http://localhost:3005/api/health` et `http://localhost:3000`, pas seulement des ports ouverts.
- Le resume de lancement distingue `health OK`, `port ouvert mais health KO`, et `pas demarre`.
- Le launcher affiche l'URL LAN/tablette `http://<ip-lan>:3000` quand une IPv4 LAN est detectee, et la met dans le toast de succes.
- Ameliore `scripts/Status-GlowLogic.ps1`: teste les ports + les endpoints HTTP, affiche HTTP status/erreur, et donne un diagnostic clair si Web OK mais API KO ou inversement.
- Verification locale: syntaxe PowerShell OK pour Launch/Status; `Status-GlowLogic.ps1` a detecte API `HTTP 200` et Web `HTTP 200` sur cette machine.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit`, `npm run test:api`, `git diff --check` OK.
- Build: Next compile, puis blocage Windows/sandbox `spawn EPERM` identique aux chantiers precedents.

## C12d DMX offline queue testable

- Extrait la logique de file DMX offline dans `apps/web/src/lib/dmxOfflineQueue.ts`.
- La file dedoublonne les writes par couple univers/canal, normalise univers/canal/valeur, serialise en version 1, restaure depuis localStorage et draine avec `lastFlushedAt`.
- `dmxEngine.ts` garde le meme comportement public mais delegue maintenant `queue`, `status`, `persist`, `restore` et `drain` au module pur.
- Ajoute `apps/web/tests/dmxOfflineQueue.test.ts` et l'integre a `npm run test:unit --workspace web`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit --workspace web`, `npm run test:api`, `git diff --check` OK.
- Build: Next compile en production puis echoue encore sur `spawn EPERM`; demande d'execution hors sandbox rejetee par l'infra d'approbation (`404 Not Found`).

## C13a smoke terrain sans Playwright

- Ajoute `apps/server/tests/field-smoke.ts`: smoke pre-show sans navigateur/Playwright.
- Le test demarre une API geree sauf si `GLOW_API_BASE` pointe vers une API existante, bootstrap le token local, puis verifie auth 401, licence offline, safety status, sync BPM, DMX output status, bibliotheque, profils de salle et export/import `.glowproject`.
- Le test cree puis nettoie ses donnees temporaires: library item, venue profile, projet exporte et projet importe.
- Optionnel: si `GLOW_WEB_BASE` est defini, le test ajoute un check HTTP frontend leger sans worker Playwright.
- Ajoute `npm run test:field --workspace server` et le script racine `npm run test:field`; `package:check` inclut maintenant `test:field`.
- Documente la commande dans `docs/DEVELOPER_GUIDE.md`.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit --workspace web`, `npm run test:api`, `npm run test:field`, `git diff --check` OK.
- Build: Next compile en production puis echoue encore sur `spawn EPERM`; demande d'execution hors sandbox rejetee par l'infra d'approbation (`404 Not Found`).

## C13b mesure Time-To-First-Light

- Ajoute `scripts/Measure-TTFL.ps1`: mesure les temps de disponibilite API health et interface web, peut demarrer les services avec `-StartServices`, peut lancer le smoke terrain avec `-RunFieldSmoke`, puis ecrit un rapport Markdown dans `reports/field/`.
- Ajoute le script racine `npm run field:ttfl`.
- Documente l'outil TTFL dans `docs/DEVELOPER_GUIDE.md`.
- Verification PowerShell: parsing du script OK; execution courte sans services demarres OK cote script mais resultat attendu `FAIL` car les ports 3005/3000 etaient indisponibles.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit --workspace web`, `npm run test:api`, `npm run test:field`, `git diff --check` OK.

## C13c rapport de validation terrain

- Ajoute `docs/FIELD_VALIDATION.md`: checklist humaine du rituel "Show de zero" avec gate automatise, etapes terrain, TTFL, decision release et issues bloquantes.
- Ajoute `scripts/New-FieldValidationReport.ps1`: genere un rapport Markdown horodate dans `reports/field/`, pre-rempli avec machine, operateur, mode, controleur et ref git.
- Ajoute le script racine `npm run field:report`.
- Documente `field:report` dans `docs/DEVELOPER_GUIDE.md`.
- Supprime les rapports temporaires de test `ttfl-smoke-test.md` et `field-report-smoke.md` crees pendant les verifications locales.
- Verification PowerShell: parsing OK; generation de rapport OK dans `reports/field/` puis nettoyage.
- Validation: `npm run typecheck --workspaces`, `npm run lint --workspace web`, `npm run test:unit --workspace web`, `npm run test:api`, `npm run test:field`, `git diff --check` OK.

## C13d gate release candidate

- Ajoute `scripts/Run-ReleaseCandidateChecks.ps1`: lance les controles RC non-Playwright en sequence, mesure les durees, detecte `spawn EPERM` comme `BLOCKED`, et ecrit un rapport Markdown dans `reports/field/`.
- Ajoute `npm run release:check`.
- Documente `release:check` dans `docs/DEVELOPER_GUIDE.md` et l'ajoute au gate automatise de `docs/FIELD_VALIDATION.md`.
- Le runner supporte les options `-IncludeBuild`, `-IncludeUi`, `-IncludeTtfl` et `-FailOnOptional` pour machines autorisees.
- Corrige le runner pour ne pas traiter les warnings CRLF de `git diff --check` comme une exception PowerShell.
- Verification: parsing PowerShell OK; `Run-ReleaseCandidateChecks.ps1 -ReportPath reports/field/release-check-smoke.md` PASS, puis rapport de test supprime.
- Validation couverte par le gate RC: typecheck, lint web, unit web, API smoke, field smoke, `git diff --check` OK.

## C13e handoff release candidate

- Ajoute `docs/RELEASE_CANDIDATE_HANDOFF.md`: resume du gate RC, commandes de handoff, optionnels build/UI/TTFL, blocages connus `spawn EPERM`, couverture automatisee et decision release.
- Met a jour `docs/DEVELOPER_GUIDE.md` pour pointer vers le handoff.
- Ajoute `reports/field/*.md` dans `.gitignore`: les rapports generes par `release:check`, `field:ttfl` et `field:report` restent des preuves locales, pas des sources a committer.

## C14a diagnostic Windows spawn EPERM

- Ajoute `scripts/Diagnose-SpawnEperm.ps1` et le script racine `npm run doctor:eperm`.
- Ajoute `docs/WINDOWS_EPERM_DIAGNOSTIC.md` avec la procedure Windows Security / Controlled Folder Access et les commandes de verification.
- Met a jour `docs/DEVELOPER_GUIDE.md` et `docs/RELEASE_CANDIDATE_HANDOFF.md` pour pointer vers `doctor:eperm`.
- Change `.gitignore` de `reports/field/*.md` a `reports/field/` pour ignorer tous les artefacts generes localement par les outils terrain.
- Verification: parsing PowerShell OK; `npm run doctor:eperm` genere un rapport et sort en code 0. Le probe confirme `child_process.spawnSync` -> `EPERM` pour `C:\Program Files\nodejs\node.exe` dans cette session; le repo est sous Desktop et `Get-MpPreference` est refuse par permissions.

## C15 audit multi-agents + 7 quick wins UX/perf

- Audit multi-agents (5 sous-agents Opus: Navigation/TopBar, Smart Dashboard, Creator/3D, Backend/DMX, Store/Offline) consigne dans `AUDIT_MULTI_AGENTS.md`.
- QW1 BPM dupliqué supprimé du header `SmartDashboard.tsx` (le BPM global reste dans `TopBar`).
- QW2 "module paramètre s'enlève": `selectFixture(id, options?)` dans `uiSlice.ts` ne force plus `isSidebarVisible`/`smartSidebarPanel`; ouverture inspecteur passee en opt-in `{ openInspector: true }`. Call-sites mis a jour: `SmartSidebar` (liste fixtures), `StagePlan` (wrapper local), `GroupInspectorPanel`. Le canvas (`AppShell`) et la 3D (`VisualizerView`/`FixtureRenderer`) ne forcent plus la sidebar.
- QW3 personnalisation Smart: remplace les 2 `useEffect` qui ecrasaient `smartWidgets` (visible/collapsed/order) par une migration one-shot au montage qui ajoute seulement les widgets manquants depuis `DEFAULT_WIDGETS` (exporte depuis `smartModeSlice.ts`).
- QW4 mojibake/accents: `DEFAULT_WIDGETS` (Plan de Scène / Contrôles de Zone), `showPlayerSlice.ts` (entree 'LatÃ©ral' redondante supprimee), `smartModeSlice.ts` (toast Scène activée/désactivée), `TopBar.tsx` (Backend connecté/déconnecté, Scène active, Laser/Pyro armé/désarmé, Vérification/Récupération, Bibliothèque).
- QW5 perf: suppression des `console.log` OSC dans le hot-path 44 Hz (`qlc.ts` sendDmxValue/triggerScene/setSlider).
- QW6 3D: suppression du raycaster global redondant `FixtureClickHandler` dans `VisualizerView.tsx` (selection deja geree par le onClick par-fixture de `FixtureRenderer`); imports `useThree`/`THREE` retires.
- QW7 perf live: cache memoire 250 ms de `getSafetyState()` dans `safetyGate.ts` (sortait des centaines de lectures SQLite synchrones du hot-path DMX), invalide sur `setOperatorRole`/`setHazardArmed`.
- Validation: `npm run typecheck --workspaces` 0 erreur, `npm run test:unit --workspace web` OK, `npm run test:api` OK (safety inclus). Pas de build (spawn EPERM connu).
